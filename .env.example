OPENAI_API_KEY=your-openai-api-key-here

# Optional login gate — leave blank for no auth (local dev).
# On Streamlit Cloud, prefer setting these via app Secrets:
#   [auth]
#   username = "admin"
#   password = "..."
APP_USERNAME=
APP_PASSWORD=
